public class Main {
    public static void main(String[] args) {

        Menu m = new Menu();
        m.showMenu();

        // AddNewAnimal a = new AddNewAnimal();
        // ShowAnimals s = new ShowAnimals();
        // a.addAnimal("N", Type.DOG);
        // a.addAnimal("T", Type.DOG);
        // a.addAnimal("N", Type.CAT);
        // a.showAnimals(Type.CAT);

        // Animal d = new Animal();
        // d.addAnimal("DI", Type.CAT);
        // d.addAnimal("fnt", Type.DOG);
        // d.addAnimal("di", Type.DOG);
        // d.showAnimals();
    }
}
